{
  "name": "ingest-with-vercel",
  "version": "1.0.0",
  "description": "A simple boilerplate for using Ingest with Vercel.",
  "private": true,
  "scripts": {
    "generate": "yarn build:tsc && yarn build:files",
    "build:tsc": "tsc",
    "build:files": "ts-node src/build.ts",
    "dev": "ts-node src/develop.ts"
  },
  "dependencies": {
    "@stackpress/ingest-vercel": "0.1.16"
  },
  "devDependencies": {
    "@types/node": "22.1.0",
    "ts-node": "10.9.2",
    "typescript": "5.5.4"
  }
}